name: Build CtrlDesk Installer

on:
  workflow_run:
    workflows: ["Build ActivityWatch Windows EXE"]
    types: [completed]
    branches: [main, master]
  workflow_dispatch:
    inputs:
      run_id:
        description: 'Run ID of build-aw workflow (leave empty for latest)'
        required: false
      server_url:
        description: 'CtrlDesk / central server URL'
        required: false
        default: ''
      company_id:
        description: 'Company ID'
        required: false
        default: ''
      tenant_id:
        description: 'Tenant ID'
        required: false
        default: ''
      installer_id:
        description: 'Installer ID'
        required: false
        default: ''
      build_id:
        description: 'Build UUID from installer-service'
        required: false
        default: ''

jobs:
  build-installer:
    runs-on: windows-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Resolve artifact run ID
        id: resolve_run
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ inputs.run_id }}" ]; then
            echo "run_id=${{ inputs.run_id }}" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" = "workflow_run" ]; then
            echo "run_id=${{ github.event.workflow_run.id }}" >> $GITHUB_OUTPUT
          else
            run_id=$(gh run list --workflow=build-aw.yml --status=success --limit=1 --json databaseId --jq '.[0].databaseId')
            echo "run_id=$run_id" >> $GITHUB_OUTPUT
          fi

      - name: Download activitywatch artifact
        uses: actions/download-artifact@v4
        with:
          name: activitywatch-windows-x64
          path: dist/activitywatch
          github-token: ${{ github.token }}
          run-id: ${{ steps.resolve_run.outputs.run_id }}

      - name: Write config_template.json
        shell: bash
        run: |
          mkdir -p dist/activitywatch
          cat > dist/activitywatch/config_template.json <<EOF
          {
            "company_id": "${{ inputs.company_id }}",
            "tenant_id": "${{ inputs.tenant_id }}",
            "server_url": "${{ inputs.server_url }}",
            "installer_id": "${{ inputs.installer_id }}",
            "build_id": "${{ inputs.build_id }}"
          }
          EOF

      - name: Copy post-install script
        shell: bash
        run: cp installer/post-install.ps1 dist/activitywatch/post-install.ps1

      - name: Ensure aw-qt tray icon assets in installer input
        shell: pwsh
        run: |
          $mediaSrc = "aw-qt\media"
          $mediaDst = "dist\activitywatch\media"
          if (-not (Test-Path $mediaSrc -PathType Container)) {
            Write-Error "Missing source tray assets: $mediaSrc"
            exit 1
          }
          if (Test-Path $mediaDst) {
            Remove-Item $mediaDst -Recurse -Force
          }
          Copy-Item -Path $mediaSrc -Destination $mediaDst -Recurse -Force

      - name: Verify aw-qt tray icon assets in downloaded artifact
        shell: pwsh
        run: pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/package/verify-windows-tray-assets.ps1 -Root dist/activitywatch

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0'

      - name: Install WiX v4
        run: |
          dotnet tool install --global wix --version "4.0.6"
          wix extension add WixToolset.UI.wixext/4.0.6
          wix extension add WixToolset.Bal.wixext/4.0.6
          wix extension add WixToolset.Util.wixext/4.0.6

      - name: Compute installer version
        id: installer_version
        shell: pwsh
        run: |
          $now = [DateTime]::UtcNow
          $major = $now.Year - 2000
          $minor = $now.Month
          $build = ($now.Day * 1440) + ($now.Hour * 60) + $now.Minute + [int]${{ github.run_attempt }}
          $version = "$major.$minor.$build"
          Write-Host "Installer version: $version"
          "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Generate file components from dist/activitywatch
        shell: pwsh
        run: pwsh -File scripts/package/generate-wix-harvest.ps1

      - name: Build MSI
        run: |
          wix build installer/Product.wxs installer/HarvestedFiles.wxs installer/TokenDlg.wxs `
            -d ProductVersion=${{ steps.installer_version.outputs.version }} `
            -arch x64 `
            -ext WixToolset.UI.wixext `
            -ext WixToolset.Util.wixext `
            -o OTGuruAgent.msi

      - name: Dump MSI dialog sequence (debug)
        shell: pwsh
        run: |
          wix msi decompile OTGuruAgent.msi -o decompiled.wxs
          Write-Host "=== NewDialog events ==="
          Get-Content decompiled.wxs | Select-String "WelcomeEula|TokenDlg|NewDialog" | Select-Object -First 40

      - name: Validate MSI (silent install test)
        shell: pwsh
        run: |
          $log = "install_test.log"
          $proc = Start-Process msiexec.exe `
            -ArgumentList "/i OTGuruAgent.msi /qn /l*v $log" `
            -Wait -PassThru
          if ($proc.ExitCode -ne 0) {
            Write-Host "MSI install failed, exit code: $($proc.ExitCode)"
            Get-Content $log | Select-String "2819|Error|error" | Select-Object -Last 30
            Write-Host "--- last 50 lines ---"
            Get-Content $log | Select-Object -Last 50
            exit 1
          }
          Write-Host "MSI validation passed"

      - name: Verify installed aw-qt tray icon assets
        shell: pwsh
        run: pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/package/verify-windows-tray-assets.ps1 -Root "C:\Program Files\ctrldesk"

      - name: Build EXE bundle
        run: |
          wix build installer/Bundle.wxs `
            -d BundleVersion=${{ steps.installer_version.outputs.version }} `
            -arch x64 `
            -ext WixToolset.Bal.wixext `
            -o agent_installer.exe

      - name: Upload EXE bundle artifact
        uses: actions/upload-artifact@v4
        with:
          name: otguru-agent-installer
          path: agent_installer.exe
          retention-days: 30

      - name: Upload MSI artifact
        uses: actions/upload-artifact@v4
        with:
          name: otguru-agent-msi
          path: OTGuruAgent.msi
          retention-days: 30
